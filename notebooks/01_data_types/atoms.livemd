# Learning Elixir: Atoms

## Atoms

```elixir
value = :atom

is_atom(value)
```

```elixir
value = false

is_atom(value)
```

```elixir
is_atom(:ok)
is_atom(:error)
```

```elixir
value = 'String'

is_atom(value)
```

```elixir
value = :String

is_atom(value)
```

```elixir
:math.pi()
```
